name: sync

on:
  schedule:
  - cron: '0 0 * * *'

  push:
    branches: 
      - 'wallhavenrequests'
      - 'main'
  pull_request:
    branches:
      - 'wallhavenrequests'
      - 'main'
  
jobs:
  run-python-script:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]

    steps:
    - uses: actions/checkout@v3
      with:
        ref: wallhavenrequests
        lfs: true
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        pip install requests
    - name: Run 爬虫
      run: |
        wget https://raw.githubusercontent.com/zknb-666/Random-ACG-API/main/pc爬虫.py
        wget https://raw.githubusercontent.com/zknb-666/Random-ACG-API/main/pe爬虫.py
        python pc爬虫.py
        python pe爬虫.py
        rm -rf pc爬虫.py
        rm -rf pe爬虫.py
    - name: commit
      run: |
        git config --global user.email 2865467965@qq.com
        git config --global user.name zknb-666
        git add .
        git commit -m "更新图库"
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        force: true
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: wallhavenrequests

